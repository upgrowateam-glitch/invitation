const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function fix() { const templates = await prisma.template.findMany(); for(let t of templates){ if (t.originalFilePath.includes('trycloudflare.com')) { const fixed = '/uploads' + t.originalFilePath.split('/uploads')[1]; await prisma.template.update({where:{id:t.id}, data:{originalFilePath:fixed}}); console.log('Fixed:', fixed); } } } fix().catch(console.error).finally(()=>prisma.$disconnect());
